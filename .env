# Render PostgreSQL connection string (use Render's Internal Database URL

# Optional MongoDB Atlas connection. This project currently uses SQLAlchemy,
# so using MongoDB requires migrating the database layer first.
DATABASE_URL=postgresql://postgres:Abhi%401289@localhost:5129/mydatabase
# Generate a long, random value; do not reuse the example value.
JWT_SECRET=replace-with-a-long-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=120
PROJECT_NAME=Career Compass AI

# Comma-separated allowed frontend origins. Use your actual Vercel production URL.
CORS_ORIGINS=https://prog-frontend-k04rpvudo-arham1008.vercel.app

# Optional: enables Gemini-backed replies; omit to use the existing local fallback.
GEMINI_API_KEY=
